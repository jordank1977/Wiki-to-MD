services:
  wiki-rag-sync:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wiki-rag-sync
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
